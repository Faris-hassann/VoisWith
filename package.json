{
  "name": "ai-uipath-flow-builder",
  "private": true,
  "version": "0.1.0",
  "description": "MVP desktop/web app for generating UiPath automations from natural language prompts.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w backend & npm run dev -w frontend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w frontend"
  }
}
